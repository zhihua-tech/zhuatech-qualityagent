# 企业级 CAPA 建议发布治理

`POST /api/enterprise/qualityagent/capa-recommendation-release` 检查不符合关联、遏制、根因证据、责任期限、验证方案、法规影响、有效性检查和变更审计，返回 `RELEASE / REVIEW / BLOCKED`。

AI 可辅助根因和措施建议，但正式 CAPA 必须由授权质量人员批准并完成有效性验证。
